# ActionLoop dependencies -- single source of truth for what needs installing.
# Install with:  pip install -r requirements.txt

# --- Web layer ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# --- Validation / configuration ---
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# --- Persistence ---
SQLAlchemy>=2.0,<3.0

# --- LLM ---
anthropic>=0.40,<1.0
httpx>=0.27,<1.0

# --- Google APIs ---
google-api-python-client>=2.140,<3.0
google-auth>=2.34,<3.0
google-auth-httplib2>=0.2,<1.0
google-auth-oauthlib>=1.2,<2.0

# --- Testing (kept here so a single install command is enough) ---
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<2.0